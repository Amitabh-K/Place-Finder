var map;

function createMap () {
  var options = {
    center: { lat: 43.557930, lng: -79.707533 },
    zoom: 5
  };
  map = new google.maps.Map(document.getElementById('map'), options);











}